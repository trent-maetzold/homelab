http:
  routers:
    unraid:
      service: unraid
      rule: "Host(`unraid.trkm.io`)"
      entryPoints:
        - websecure
  services:
    unraid:
      loadBalancer:
        servers:
          - url: "http://ymir.ts.trkm.io:80"
