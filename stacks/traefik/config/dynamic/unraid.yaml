http:
  routers:
    unraid:
      service: unraid
      rule: "Host(`unraid.trkm.io`)"
      entryPoints:
        - websecure
  services:
    unraid:
      loadBalancer:
        servers:
          - url: "http://100.64.0.1:80"
