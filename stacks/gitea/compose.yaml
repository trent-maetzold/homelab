# yaml-language-server: $schema=https://raw.githubusercontent.com/compose-spec/compose-spec/master/schema/compose-spec.json

services:
  app:
    container_name: gitea
    image: gitea/gitea:latest
    restart: unless-stopped
    ports:
      - "222:22"
    volumes:
      - /mnt/cache/appdata/gitea/app/config:/data
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    networks:
      - net
      - proxy
    environment:
      TZ: "America/Chicago"
      GITEA__database__DB_TYPE: "postgres"
      GITEA__database__HOST: "db:5432"
      GITEA__database__NAME: "gitea"
      GITEA__database__USER: "gitea"
      GITEA__database__PASSWD: "${POSTGRES_PASSWORD}"
      GITEA__server__START_SSH_SERVER: "true"
      GITEA__server__SSH_DOMAIN: "tea.trkm.io"
      GITEA__server__SSH_PORT: "222"
      GITEA__pages__ENABLED: "true"
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "--quiet", "http://localhost:3000/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    labels:
      traefik.enable: "true"
      traefik.http.routers.gitea.rule: "Host(`tea.trkm.io`)"
      traefik.http.services.gitea.loadBalancer.server.port: "3000"

  db:
    container_name: gitea_db
    image: postgres:18
    restart: unless-stopped
    volumes:
      - /mnt/cache/dbdata/postgres/gitea/data:/var/lib/postgresql
    networks:
      - net
    environment:
      TZ: "America/Chicago"
      POSTGRES_DB: "gitea"
      POSTGRES_USER: "gitea"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}"
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "gitea"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

networks:
  net:
  proxy:
    external: true
