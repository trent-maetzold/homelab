# yaml-language-server: $schema=https://raw.githubusercontent.com/compose-spec/compose-spec/master/schema/compose-spec.json

services:
  server:
    image: caddy:latest
    restart: unless-stopped
    volumes:
      - /mnt/cache/data/artifacts:/srv/artifacts:ro
    networks:
      - proxy
    healthcheck:
      test: ["CMD-SHELL", "wget --spider --quiet http://localhost/ || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    command: caddy file-server --browse --root /srv --listen :80
    labels:
      traefik.enable: "true"
      traefik.http.routers.artifacts.rule: "Host(`caddy.trkm.io`)"
      traefik.http.services.artifacts.loadBalancer.server.port: "80"

networks:
  proxy:
    external: true
