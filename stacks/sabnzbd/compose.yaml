# yaml-language-server: $schema=https://raw.githubusercontent.com/compose-spec/compose-spec/master/schema/compose-spec.json

services:
  sabnzbd:
    container_name: sabnzbd
    image: lscr.io/linuxserver/sabnzbd:latest
    restart: unless-stopped
    volumes:
      - /mnt/user/appdata/sabnzbd/config:/config
      - /mnt/user/data/usenet:/data/usenet
    network_mode: "service:vpn"
    environment:
      PUID: "99"
      PGID: "100"
      UMASK: "022"
      TZ: "America/Chicago"
    depends_on:
      - vpn
    labels:
      net.unraid.docker.icon: "https://raw.githubusercontent.com/homarr-labs/dashboard-icons/refs/heads/main/png/sabnzbd.png"
      net.unraid.docker.webui: "https://nzb.trkm.io"
      net.unraid.docker.shell: "/bin/bash"

  vpn:
    container_name: sabnzbd_vpn
    image: qmcgaw/gluetun:latest
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    volumes:
      - /mnt/user/appdata/sabnzbd/vpn:/gluetun
    networks:
      - pvr
      - proxy
    environment:
      TZ: "America/Chicago"
      VPN_SERVICE_PROVIDER: "protonvpn"
      VPN_TYPE: "wireguard"
      WIREGUARD_PRIVATE_KEY: "${WIREGUARD_PRIVATE_KEY}"
      SERVER_CITIES: "Chicago"
    labels:
      traefik.enable: "true"
      traefik.http.routers.sabnzbd.rule: "Host(`nzb.trkm.io`)"
      traefik.http.services.sabnzbd.loadBalancer.server.port: "8080"
      net.unraid.docker.icon: "https://raw.githubusercontent.com/homarr-labs/dashboard-icons/refs/heads/main/png/gluetun.png"
      net.unraid.docker.shell: "/bin/sh"

networks:
  pvr:
    external: true
  proxy:
    external: true
