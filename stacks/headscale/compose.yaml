# yaml-language-server: $schema=https://raw.githubusercontent.com/compose-spec/compose-spec/master/schema/compose-spec.json

services:
  headscale:
    container_name: headscale
    image: headscale/headscale:latest
    restart: unless-stopped
    volumes:
      - /mnt/user/appdata/headscale/config:/etc/headscale
      - /mnt/user/appdata/headscale/data:/var/lib/headscale
    networks:
      - proxy
    command: serve
    labels:
      me.tale.headplane.target: headscale
      traefik.enable: true
      traefik.http.routers.headscale.rule: Host(`hs.trkm.io`)
      traefik.http.services.headscale.loadBalancer.server.port: 8080

  headplane:
    container_name: headplane
    image: ghcr.io/tale/headplane:latest
    restart: unless-stopped
    volumes:
      - /mnt/user/appdata/headplane/config:/etc/headplane
      - /mnt/user/appdata/headplane/data:/var/lib/headplane
      - /mnt/user/appdata/headscale/config:/etc/headscale
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - proxy
    depends_on:
      - headscale
    healthcheck:
      test: ["/bin/hp_healthcheck"]
      interval: 30s
      timeout: 5s
      start_period: 5s
      retries: 3
    labels:
      traefik.enable: true
      traefik.http.routers.headplane.rule: Host(`hs.trkm.io`) && PathPrefix(`/admin`)
      traefik.http.services.headplane.loadBalancer.server.port: 3000

networks:
  proxy:
    external: true
